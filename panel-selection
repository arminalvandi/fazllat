<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>انتخاب پنل | مدرسه فضیلت</title>
  <link href="https://fonts.googleapis.com/css2?family=Vazirmatn&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" integrity="sha512-RXf+QSDCUQs6Q0AZK0P5UnNLOy1mOrrVxKDx6X36ZD/Vs2BBytjR7J/E69BkVY9xT+4mOMZAA0d/a47z1w==" crossorigin="anonymous" referrerpolicy="no-referrer" />
  <style>
    body {
      font-family: 'Vazirmatn', sans-serif;
      margin: 0;
      background: linear-gradient(to bottom, #ffffff, #e0f7fa);
      color: #333;
    }

    header {
      background-color: #00796b;
      color: white;
      padding: 1rem;
      text-align: center;
      font-size: 1.5rem;
      font-weight: bold;
      box-shadow: 0 2px 6px rgba(0,0,0,0.2);
    }

    .panel-container {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 2rem;
      padding: 3rem 2rem;
      max-width: 900px;
      margin: auto;
    }

    .panel-button {
      background: white;
      border-radius: 15px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.1);
      padding: 2rem 1rem;
      text-align: center;
      transition: 0.3s ease;
      cursor: pointer;
      border: 2px solid #00796b;
    }

    .panel-button:hover {
      transform: scale(1.05);
      background-color: #e0f2f1;
    }

    .panel-button i {
      font-size: 2.8rem;
      color: #00796b;
      margin-bottom: 1rem;
    }

    .panel-button span {
      display: block;
      font-size: 1.1rem;
      font-weight: bold;
      color: #333;
    }

    footer {
      background-color: #004d40;
      color: white;
      text-align: center;
      padding: 1rem;
      font-size: 0.9rem;
      margin-top: 3rem;
    }

    @media (max-width: 768px) {
      .panel-container {
        grid-template-columns: repeat(2, 1fr);
      }
    }

    @media (max-width: 500px) {
      .panel-container {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>

  <header>انتخاب پنل کاربری</header>

  <section class="panel-container">

    <div class="panel-button" onclick="alert('ورود به پنل سوپرمدیر')">
      <i class="fas fa-crown"></i>
      <span>سوپرمدیر</span>
    </div>

    <div class="panel-button" onclick="alert('ورود به پنل مدیر')">
      <i class="fas fa-user-tie"></i>
      <span>مدیر</span>
    </div>

    <div class="panel-button" onclick="alert('ورود به پنل معاون')">
      <i class="fas fa-user-cog"></i>
      <span>معاون</span>
    </div>

    <div class="panel-button" onclick="alert('ورود به پنل مشاور')">
      <i class="fas fa-user-md"></i>
      <span>مشاور</span>
    </div>

    <div class="panel-button" onclick="alert('ورود به پنل معلم')">
      <i class="fas fa-chalkboard-teacher"></i>
      <span>معلم</span>
    </div>

    <div class="panel-button" onclick="alert('ورود به پنل دانش‌آموز')">
      <i class="fas fa-user-graduate"></i>
      <span>دانش‌آموز</span>
    </div>

  </section>

  <footer>
    © مدرسه فضیلت - تمامی حقوق محفوظ است
  </footer>

</body>
</html>
